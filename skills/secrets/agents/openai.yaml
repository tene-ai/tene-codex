interface:
  display_name: "tene Secrets"
  short_description: "Run commands without exposing secrets"
  default_prompt: "Use $tene:secrets to run this test through the tene secret boundary."
policy:
  allow_implicit_invocation: true

