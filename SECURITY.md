# Security

Report vulnerabilities privately to kay@agentkay.it. Do not include live credentials. The plugin never requires secret values in chat and uses the tene CLI boundary for secret-dependent commands. Supported security fixes target the latest release line.
